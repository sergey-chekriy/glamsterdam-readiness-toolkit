{
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "engines": {"node": ">=22"},
  "scripts": {
    "build": "npm run -ws build",
    "dev": "npm run -w @grt/cli dev",
    "typecheck": "npm run -ws typecheck",
    "test": "npm run -ws test",
    "lint": "npm run -ws lint"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "tsx": "^4.19.4",
    "vitest": "^3.1.2",
    "tsup": "^8.5.0",
    "eslint": "^9.25.1",
    "@types/node": "^22.15.3"
  }
}
